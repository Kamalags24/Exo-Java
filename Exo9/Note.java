public class Note {
    public static void main(String[]args){
        int note = 75;

        String calcul;
        calcul = (note >= 50) ? "Réussi" : "Échoué";
        System.out.println(calcul);
    }
}